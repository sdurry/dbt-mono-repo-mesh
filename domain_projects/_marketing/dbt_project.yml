config-version: 2

name: marketing
version: 2.0
profile: tpch

dbt-cloud:
  project-id: '13285'
  account-host: "pp569.eu1.dbt.com"

seed-paths:
  - data
model-paths:
  - models

clean-targets:
  - "target"
  - "dbt_packages"

models:
  +automatic_clustering: true
  +query_tag: stephan-marketing
  +persist_docs:
    relation: true
    columns: true
  +copy_grants: true

flags:
  require_generic_test_arguments_property: true
